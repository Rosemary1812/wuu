import type { PartialOptions } from "overlayscrollbars";

export const OVERLAY_SCROLLBAR_OPTIONS = {
  scrollbars: {
    autoHide: "leave",
    autoHideDelay: 360,
    clickScroll: true,
    theme: "os-theme-wuu"
  }
} satisfies PartialOptions;
