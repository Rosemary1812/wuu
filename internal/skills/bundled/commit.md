---
name: commit-redirect
description: Redirect notice — the commit skill moved to commit/SKILL.md in this directory.
user-invocable: false
---

This skill moved to `internal/skills/bundled/commit/SKILL.md`. The new
location follows the standard `SKILL.md` layout used by the other 10
bundled skills (trigger-condition, required-context, examples,
verification-checklist, progressive-disclosure). This file is kept only
so older references resolve to a clear redirect rather than silently
disappearing. Invoke `/commit` against the new path.